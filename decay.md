# decay
为什么要进行类型衰退？
当函数参数是数组或函数类型时，它们会自动转换为指针类型。

# 目的
类型衰退的目的是为了**兼容性**和**效率**

当你传递一个数组给函数时，如果不进行类型衰退，就需要复制整个数组的内容，这在大多数情况下是非常低效的。通过将数组转换为指针，只需要传递一个地址，大大提高了效率。

此外，类型衰退也保持了与C语言的兼容性，因为在C语言中，数组在传递给函数时也会退化为指针。

- 